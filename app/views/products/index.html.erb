<section id="products">
  <h1><u>Bikes For Sale</u></h1>
  <div class="container-fluid">
    <div class="row">
      <% @products.each do |product| %>
        <div class="col-md-6 col-lg-6">
          <h3><%= product.name %></h3>
          <p>£<%= product.price %></p>
         <%= image_tag(product.image_url, class: "img-responsive", id: "folders") %>
          <p><%= product.description %></p>
        </div><!-- /col -->
      <% end %>
    </div>
  </div>

<br>

<%= link_to 'New Product', new_product_path %>