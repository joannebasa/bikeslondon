<% if signed_in? %>
  <h4>Add a Review:</h4>
  <%= form_for([@product, @product.comments.build]) do |f| %>
    <p>
      <%= f.label :body, "Add a Review:" %><br>
      <%= f.text_area :body %>
    </p>
    <p>
      <div class="rating"></div>
    </p>
    <p>
      <%= f.submit "Submit", class: 'btn btn-info' %>
    </p>
  <% end %>
<% end %>